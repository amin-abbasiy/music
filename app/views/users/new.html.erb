<%= provide(:title, "SignUp") %>
<%= provide(:text, "SignUp")%>
   <%= render("form") %>
   <% # :password and :password_confirmation is bcrypt has_secure_password 
      # in database is password_digest and in model :password 
      # this both convert to password_digest in database and  
    %>
